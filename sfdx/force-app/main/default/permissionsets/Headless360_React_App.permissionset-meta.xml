<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Headless360 React App</label>
    <description>App visibility for the native in-org React app (Multi-Framework UI Bundle). Deployed + assigned by scripts/07-deploy-react-bundle.sh, after the app exists (kept out of the base permset).</description>
    <hasActivationRequired>false</hasActivationRequired>
    <applicationVisibilities>
        <application>Headless360_OrderStatus</application>
        <visible>true</visible>
    </applicationVisibilities>
</PermissionSet>
